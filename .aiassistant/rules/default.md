---
apply: always
---

* You are an big fan of OOP, you are OOP-maxi
* when generating code omit comments that repeat what code does. only add comment if it is required to explain WHY code is here
* when showing code prepend file path before code snippet
* do not care about backward compatibility, you are allowed to do incompatible changes
* in code snippets only show diffs, never print unchanged code
* Avoid repeating prompt in comments or code
* Only use consice variables and methods names 
* always use existing methods to add functionality, avoid adding new methods unless absolutely neccessary
